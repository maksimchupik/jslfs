# 🔨 Инструкция по сборке exe файла

## Быстрая сборка

### Шаг 1: Установка PyInstaller
```bash
pip install pyinstaller
```

### Шаг 2: Сборка exe

#### Вариант A: Без консоли (для пользователей)
```bash
python build_exe.py
```

#### Вариант B: С консолью (для отладки)
```bash
python build_exe_with_console.py
```

### Шаг 3: Результат
Exe файл будет в папке `dist/TelegramAccountsControl.exe`

---

## Подготовка к распространению

### Что нужно включить в дистрибутив:

1. **Exe файл:** `TelegramAccountsControl.exe`

2. **Файл .env** (пользователь создаст сам, но можно включить шаблон):
   - См. `INSTALLATION_GUIDE.md` раздел "Настройка конфигурации"

3. **Папка web/** (если не включена в exe):
   - Скопируйте папку `web` рядом с exe файлом

4. **Инструкция для пользователя:**
   - Скопируйте `INSTALLATION_GUIDE.md` или создайте краткую версию

### Структура дистрибутива:

```
TelegramAccountsControl/
├── TelegramAccountsControl.exe
├── web/                          # Если не включена в exe
│   ├── index.html
│   ├── styles.css
│   └── script.js
├── .env                          # Пользователь создаст сам
└── README.txt                    # Краткая инструкция
```

---

## Решение проблем при сборке

### Проблема: "PyInstaller не найден"
```bash
pip install pyinstaller
```

### Проблема: "ModuleNotFoundError при запуске exe"
- Убедитесь, что все зависимости указаны в `--hidden-import`
- Используйте `--collect-all` для больших библиотек

### Проблема: "Веб-интерфейс не работает в exe"
- Убедитесь, что папка `web` включена через `--add-data`
- Или скопируйте папку `web` рядом с exe

### Проблема: "Exe файл слишком большой"
- Это нормально для Python приложений (50-200 МБ)
- Можно использовать `--exclude-module` для исключения ненужных модулей

---

## Оптимизация размера exe

Если нужно уменьшить размер:

1. **Исключите ненужные модули:**
   ```python
   "--exclude-module=matplotlib",
   "--exclude-module=numpy",
   ```

2. **Используйте UPX сжатие:**
   - Скачайте UPX: https://upx.github.io/
   - Добавьте в PATH
   - PyInstaller автоматически использует UPX

3. **Создайте spec файл для тонкой настройки:**
   ```bash
   pyinstaller --name=TelegramAccountsControl main.py --onefile
   ```
   Затем отредактируйте `TelegramAccountsControl.spec`

---

## Тестирование exe

1. **Создайте тестовую папку**
2. **Скопируйте exe и .env файл**
3. **Запустите exe**
4. **Проверьте:**
   - Запуск без ошибок
   - Веб-интерфейс открывается
   - API работает
   - Создание аккаунтов работает

---

**Готово!** 🎉

