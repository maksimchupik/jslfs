# 📘 Подробная инструкция по настройке и использованию

## Содержание
1. [Подготовка к работе](#подготовка-к-работе)
2. [Установка Python и зависимостей](#установка-python-и-зависимостей)
3. [Получение Telegram API credentials](#получение-telegram-api-credentials)
4. [Создание сессии для Telegram аккаунта](#создание-сессии-для-telegram-аккаунта)
5. [Настройка конфигурации](#настройка-конфигурации)
6. [Запуск программы](#запуск-программы)
7. [Использование веб-интерфейса](#использование-веб-интерфейса)
8. [Сборка exe файла](#сборка-exe-файла)
9. [Решение проблем](#решение-проблем)

---

## Подготовка к работе

### Что вам понадобится:
- ✅ Компьютер с Windows 10/11
- ✅ Интернет-соединение
- ✅ Telegram аккаунт (номер телефона)
- ✅ OpenAI API ключ (для работы LLM)
- ✅ Python 3.9 или выше (если собираете exe самостоятельно)

---

## Установка Python и зависимостей

### Шаг 1: Установка Python

1. **Скачайте Python:**
   - Перейдите на https://www.python.org/downloads/
   - Скачайте последнюю версию Python 3.9+ (рекомендуется 3.11)
   - **ВАЖНО:** При установке отметьте галочку "Add Python to PATH"

2. **Проверьте установку:**
   - Откройте командную строку (Win+R → введите `cmd` → Enter)
   - Введите: `python --version`
   - Должна отобразиться версия Python (например, Python 3.11.0)

### Шаг 2: Установка зависимостей

1. **Откройте командную строку в папке проекта:**
   - Перейдите в папку с программой
   - Зажмите Shift и нажмите правой кнопкой мыши
   - Выберите "Открыть окно PowerShell здесь" или "Открыть окно команд здесь"

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```
   
   Это установит все необходимые библиотеки:
   - telethon (для работы с Telegram)
   - fastapi и uvicorn (для веб-интерфейса)
   - openai (для работы с LLM)
   - и другие

3. **Проверьте установку:**
   ```bash
   python -c "import telethon; print('OK')"
   ```

---

## Получение Telegram API credentials

### Шаг 1: Регистрация приложения в Telegram

1. **Откройте браузер и перейдите:**
   ```
   https://my.telegram.org/apps
   ```

2. **Войдите в свой Telegram аккаунт:**
   - Введите номер телефона
   - Введите код подтверждения из Telegram

3. **Создайте приложение:**
   - Заполните форму:
     - **App title:** Любое название (например, "My Bot")
     - **Short name:** Короткое имя (например, "mybot")
     - **Platform:** Desktop
     - **Description:** Описание (можно оставить пустым)
   - Нажмите "Create application"

4. **Скопируйте данные:**
   - **api_id** — это число (например, 12345678)
   - **api_hash** — это строка (например, "abcdef1234567890abcdef1234567890")
   
   ⚠️ **ВАЖНО:** Сохраните эти данные! Они понадобятся позже.

---

## Создание сессии для Telegram аккаунта

### Шаг 1: Подготовка скрипта

1. **Откройте файл `examples/create_session.py`**

2. **Замените значения:**
   ```python
   API_ID = 12345678  # Ваш API ID из предыдущего шага
   API_HASH = "your_api_hash_here"  # Ваш API Hash
   ```

### Шаг 2: Запуск скрипта

1. **Откройте командную строку в папке проекта**

2. **Запустите скрипт:**
   ```bash
   python examples/create_session.py
   ```

3. **Следуйте инструкциям:**
   - Введите номер телефона (с кодом страны, например: +79991234567)
   - Введите код подтверждения из Telegram
   - Если включена двухфакторная аутентификация, введите пароль

4. **Скопируйте SESSION STRING:**
   - После успешного входа появится строка вида:
     ```
     1BVtsOHwBu5...
     ```
   - **Скопируйте всю строку полностью!**
   - Сохраните её в безопасном месте

---

## Настройка конфигурации

### Шаг 1: Создание файла .env

1. **Создайте файл `.env` в корневой папке проекта**
   - Можно использовать Блокнот или любой текстовый редактор
   - Сохраните файл с именем `.env` (без расширения!)

2. **Добавьте в файл следующие строки:**
   ```env
   # Telegram API credentials
   TELEGRAM_API_ID=12345678
   TELEGRAM_API_HASH=your_api_hash_here
   TELEGRAM_SESSION_STRING=1BVtsOHwBu5...ваша_полная_строка_сессии
   
   # OpenAI API key (получите на https://platform.openai.com/api-keys)
   OPENAI_API_KEY=sk-proj-...ваш_ключ
   
   # Настройки сервера (опционально)
   PORT=8000
   HOST=0.0.0.0
   LLM_MODEL=gpt-4o-mini
   ```

3. **Замените значения:**
   - `TELEGRAM_API_ID` — ваш API ID
   - `TELEGRAM_API_HASH` — ваш API Hash
   - `TELEGRAM_SESSION_STRING` — строка сессии из предыдущего шага
   - `OPENAI_API_KEY` — ваш OpenAI API ключ (см. ниже)

### Шаг 2: Получение OpenAI API ключа

1. **Перейдите на:**
   ```
   https://platform.openai.com/api-keys
   ```

2. **Войдите или зарегистрируйтесь**

3. **Создайте новый API ключ:**
   - Нажмите "Create new secret key"
   - Скопируйте ключ (он начинается с `sk-proj-` или `sk-`)
   - ⚠️ **ВАЖНО:** Ключ показывается только один раз! Сохраните его.

4. **Добавьте ключ в файл .env**

### Шаг 3: Проверка файла .env

Ваш файл `.env` должен выглядеть примерно так:
```env
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
TELEGRAM_SESSION_STRING=1BVtsOHwBu5abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ
OPENAI_API_KEY=sk-proj-abcdefghijklmnopqrstuvwxyz1234567890
PORT=8000
HOST=0.0.0.0
LLM_MODEL=gpt-4o-mini
```

⚠️ **ВАЖНО:** 
- Не используйте кавычки вокруг значений
- Не добавляйте пробелы вокруг знака `=`
- Каждое значение на отдельной строке

---

## Запуск программы

### Вариант 1: Запуск из исходного кода

1. **Откройте командную строку в папке проекта**

2. **Запустите программу:**
   ```bash
   python main.py
   ```

3. **Дождитесь сообщения:**
   ```
   ✅ Система запущена!
   📊 Веб-интерфейс доступен по адресу: http://localhost:8000
   ```

4. **Откройте браузер и перейдите:**
   ```
   http://localhost:8000
   ```

### Вариант 2: Запуск exe файла

1. **Дважды кликните на `TelegramAccountsControl.exe`**

2. **Дождитесь запуска** (может занять несколько секунд)

3. **Откройте браузер и перейдите:**
   ```
   http://localhost:8000
   ```

⚠️ **ВАЖНО для exe:**
- Файл `.env` должен находиться в той же папке, что и exe файл
- Папка `web` должна быть рядом с exe (если не включена в exe)

---

## Использование веб-интерфейса

### Основные функции

#### 1. Просмотр аккаунтов
- На вкладке "Аккаунты" отображается список всех зарегистрированных аккаунтов
- Каждый аккаунт показывает:
  - ID аккаунта
  - Номер телефона
  - Статус (Активен/Неактивен)

#### 2. Создание нового аккаунта
1. Перейдите на вкладку "Создать аккаунт"
2. Заполните форму:
   - **Номер телефона:** +79991234567
   - **Session String:** строка сессии (из шага "Создание сессии")
   - **API ID:** ваш Telegram API ID
   - **API Hash:** ваш Telegram API Hash
3. Нажмите "Создать аккаунт"

#### 3. Управление аккаунтом
- **Запустить:** запускает аккаунт (начинает слушать сообщения)
- **Остановить:** останавливает аккаунт
- **Детали:** показывает подробную информацию и статистику
- **Профиль:** просмотр и редактирование профиля личности
- **Память:** просмотр истории чатов

#### 4. Настройки
- На вкладке "Настройки" можно изменить URL API сервера
- По умолчанию: `http://localhost:8000`

### Работа с профилем личности

1. **Откройте профиль аккаунта:**
   - Нажмите кнопку "👤 Профиль" на карточке аккаунта

2. **Просмотр настроек:**
   - Базовые настройки (стиль речи, длина сообщений, использование emoji)
   - Ограничения (блокировка личности, автономность)

3. **Управление:**
   - **Заблокировать личность:** предотвращает автоматические изменения
   - **Разблокировать личность:** разрешает эволюцию личности

---

## Сборка exe файла

### Шаг 1: Установка PyInstaller

```bash
pip install pyinstaller
```

### Шаг 2: Сборка exe

#### Вариант A: Сборка без консоли (рекомендуется для пользователей)

```bash
python build_exe.py
```

#### Вариант B: Сборка с консолью (для отладки)

```bash
python build_exe_with_console.py
```

### Шаг 3: Готовый exe файл

После сборки exe файл будет находиться в папке `dist/`:
- `dist/TelegramAccountsControl.exe`

### Шаг 4: Подготовка к распространению

Для работы exe файла нужно:

1. **Скопировать exe файл** в отдельную папку

2. **Создать файл .env** в той же папке (см. раздел "Настройка конфигурации")

3. **Скопировать папку `web`** (если не включена в exe)

4. **Создать инструкцию для пользователя:**
   - См. раздел "Запуск программы" → "Вариант 2"

---

## Решение проблем

### Проблема: "ModuleNotFoundError"

**Решение:**
```bash
pip install -r requirements.txt
```

### Проблема: "Файл .env не найден"

**Решение:**
1. Убедитесь, что файл `.env` находится в той же папке, что и `main.py` (или exe)
2. Убедитесь, что файл называется именно `.env` (не `.env.txt`)

### Проблема: "Ошибка подключения к Telegram"

**Решение:**
1. Проверьте правильность `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
2. Проверьте правильность `TELEGRAM_SESSION_STRING`
3. Попробуйте создать новую сессию (см. "Создание сессии")

### Проблема: "Ошибка при работе с OpenAI"

**Решение:**
1. Проверьте правильность `OPENAI_API_KEY`
2. Убедитесь, что на счету OpenAI есть средства
3. Проверьте лимиты API на https://platform.openai.com/usage

### Проблема: "Веб-интерфейс не открывается"

**Решение:**
1. Убедитесь, что программа запущена
2. Проверьте, что порт 8000 не занят другим приложением
3. Попробуйте изменить порт в `.env`: `PORT=8001`
4. Откройте: `http://localhost:8001`

### Проблема: "Exe файл не запускается"

**Решение:**
1. Используйте версию с консолью для отладки
2. Проверьте, что все зависимости установлены
3. Убедитесь, что файл `.env` находится рядом с exe

### Проблема: "Аккаунт не отвечает на сообщения"

**Решение:**
1. Убедитесь, что аккаунт запущен (статус "Активен")
2. Проверьте настройки профиля личности
3. Проверьте, что `OPENAI_API_KEY` правильный и работает
4. Посмотрите логи в консоли (если запущено с консолью)

---

## Дополнительная информация

### Структура папок после установки

```
софт/
├── main.py                    # Главный файл
├── .env                       # Конфигурация (создайте сами)
├── requirements.txt          # Зависимости
├── build_exe.py              # Скрипт сборки exe
├── web/                       # Веб-интерфейс
│   ├── index.html
│   ├── styles.css
│   └── script.js
├── user_accounts_system/      # Основной код системы
├── examples/                  # Примеры использования
├── data/                      # База данных (создается автоматически)
│   └── accounts.db
└── dist/                      # Exe файл после сборки
    └── TelegramAccountsControl.exe
```

### Переменные окружения (.env)

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `TELEGRAM_API_ID` | Telegram API ID | ✅ Да |
| `TELEGRAM_API_HASH` | Telegram API Hash | ✅ Да |
| `TELEGRAM_SESSION_STRING` | Строка сессии | ✅ Да |
| `OPENAI_API_KEY` | OpenAI API ключ | ✅ Да |
| `PORT` | Порт веб-сервера | ❌ Нет (по умолчанию 8000) |
| `HOST` | Хост веб-сервера | ❌ Нет (по умолчанию 0.0.0.0) |
| `LLM_MODEL` | Модель LLM | ❌ Нет (по умолчанию gpt-4o-mini) |

### API Endpoints

Веб-интерфейс использует REST API. Документация доступна по адресу:
```
http://localhost:8000/docs
```

Основные endpoints:
- `GET /accounts` - Список аккаунтов
- `POST /accounts` - Создать аккаунт
- `POST /accounts/{id}/start` - Запустить аккаунт
- `POST /accounts/{id}/stop` - Остановить аккаунт
- `GET /accounts/{id}/profile` - Получить профиль
- `PUT /accounts/{id}/profile` - Обновить профиль
- `GET /accounts/{id}/stats` - Статистика аккаунта

---

## Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем"
2. Убедитесь, что все шаги из инструкции выполнены правильно
3. Проверьте логи программы (если запущено с консолью)

---

**Удачной работы! 🚀**

