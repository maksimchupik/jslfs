# 🚀 Быстрый старт

## Минимальная инструкция для начала работы

### 1. Установка зависимостей

**Самый простой способ:**
- Дважды кликните на файл `install.bat`

**Или через командную строку:**
```bash
python install_dependencies.py
```

**Или вручную:**
```bash
pip install -r requirements.txt
```

📄 **Подробнее:** см. файл `УСТАНОВКА.txt`

### 2. Получение Telegram API
1. Перейдите: https://my.telegram.org/apps
2. Создайте приложение
3. Скопируйте `api_id` и `api_hash`

### 3. Создание сессии
```bash
python examples/create_session.py
```
- Замените `API_ID` и `API_HASH` в файле перед запуском
- Скопируйте полученную `SESSION STRING`

### 4. Создание .env файла
Создайте файл `.env` в корне проекта:
```env
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_SESSION_STRING=ваша_сессия
OPENAI_API_KEY=ваш_openai_ключ
```

Получить OpenAI ключ: https://platform.openai.com/api-keys

### 5. Запуск
```bash
python main.py
```

### 6. Открыть веб-интерфейс
```
http://localhost:8000
```

---

## Сборка exe

```bash
python build_exe.py
```

Exe файл будет в папке `dist/`

---

📘 **Подробная инструкция:** см. `INSTALLATION_GUIDE.md`

